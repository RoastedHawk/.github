# Security Policy

- Enable Secret scanning and Push protection in repo settings.
- CI runs with read-only tokens by default; write is limited to docs badge updates.
- Report suspected issues via GitHub Issues.
